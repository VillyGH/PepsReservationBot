export type OptionElement = {
    text: string | null;
    value: string | null;
}

export type ScheduleRows = {
    location: string | null,
    time: string | null,
    terrain: string | null,
    dataCountdown: any,
    btnHref: string | null
}
